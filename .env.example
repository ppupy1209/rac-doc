# 호스트 포트 오버라이드 (컨테이너 내부 포트는 그대로, 호스트 노출 포트만 변경)
# 로컬에 이미 MySQL(3306)이나 Grafana(3000) 등이 떠 있어 충돌하면
# 이 파일을 `.env`로 복사하고 값을 바꾼 뒤 `docker compose up` 하세요.
APP_PORT=8080
MYSQL_PORT=3306
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
